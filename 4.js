const countPhrase = (phrase, findText) => {
    let stringContainCount;
    let pattern = `/${findText}/`;
    // WIP

};

const reverseText = (text) => {
    return text.split("").reverse().join("");
};

countPhrase("bandanadana", "dana" );